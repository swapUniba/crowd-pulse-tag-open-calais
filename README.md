crowd-pulse-tag-open-calais
===========================

OpenCalais Crowd Pulse message tagger.

---------------------------

The `opencalais` plugin needs a `opencalais.properties` file with a `opencalais.key` property.
